# ==============================================================
# CI with GitHub Actions
# ==============================================================

name: GalaxyChop CI

on:
  push:
    branches: 
      - "dev"
  pull_request:
      branches: 
      - "dev"

env:
  default-python: '3.11'

jobs:
  testing:
    uses: BrunoCeliz/galaxy-chop/blob/dev/.github/workflows/tests.yml
  PyPI publish:
    uses: BrunoCeliz/galaxy-chop/blob/dev/.github/workflows/publish.yml
  GalaxyChop CI:
    uses: BrunoCeliz/galaxy-chop/blob/dev/.github/workflows/CI.yml 