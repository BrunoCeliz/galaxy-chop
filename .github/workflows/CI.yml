# ==============================================================
# CI with GitHub Actions
# ==============================================================

name: GalaxyChop CI

on:
  - push
  - pull_request

env:
  default-python: '3.11'

jobs:
  testing:
    uses: "BrunoCeliz/galaxy-chop/.github/workflows/tests.yml@9f536a9c1f06f43f6af387fe9a7a0bd1f7c7b557"
  