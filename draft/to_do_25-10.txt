# Bruno:

- Mejorar funcionalidades de métodos viejos (centrar sin pot, considerando velocidad, etc).
- Si los métodos transform ya están, corroborar que todo ande y se pueda hacer un pipeline ->
La pipeline se deberá hacer en una carpeta aparte (dentro de /galaxychop...) a partir del 
código fuente de skcriteria (NO importar, ya que hay que realizar modificaciones);
* Respecto al smr_crop, rev si realmente queremos una galaxia nueva pero "cortada", 
lo importante es el mismo valor del r_half_mass (como máscara, quizá...).
- Implementar que una galaxia está "compuesta" y "descompuesta", más que generar 
las "componentes" por separado... (clase "Components"). Para atacar el problema
se me ocurrió tocar la clase Galaxy (¡con cuidado!) o agregar una "columna" al 
dataframe que forma las galaxias que contenga el valor de "label" de c/ partícula
(sin tocar "ParticleSet", porque es una propiedad a nivel sistema...)