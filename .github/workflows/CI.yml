# ==============================================================
# CI with GitHub Actions
# ==============================================================

name: GalaxyChop CI

on:
  - push
  - pull_request

env:
  default-python: '3.11'

jobs:
  testing:
    uses: "BrunoCeliz/galaxy-chop/.github/workflows/tests.yml@ec64faa07184298536f090fb4be0402380f60db9"
  