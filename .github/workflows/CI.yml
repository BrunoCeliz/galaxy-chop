# ==============================================================
# CI with GitHub Actions
# ==============================================================

name: GalaxyChop CI

on:
  push:
    branches: 
      - "dev"
  pull_request:
      branches: 
      - "dev"

env:
  default-python: '3.11'

jobs:
  testing:
    uses: BrunoCeliz/galaxy-chop/.github/workflows/tests.yml@3f70f2083aa0f2f25d9ff99e6776e86385a51afd
  